import styled from 'styled-components';

export const Container = styled.div`
  display:flex;
  justify-content: space-between;
  padding: 11px 13px;
  background: var(--border-banner);
`;


export const SearchForm = styled.form`

`; 
export const TopFictionList = styled.ol`

`;